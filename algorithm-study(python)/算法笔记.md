每日三题，前期2简单1中等 中期1简单2中等 后期2中等1困难节奏
算法用时测算
```python
import time
t0 = time.perf_counter()
# 你的算法
algorithem

t1 = time.perf_counter()
print(f"耗时: {t1 - t0:.6f} 秒")
```
# day1 
# day2 
### 3838带权单词映射
python 字符与ascll互转
```
ord 字母转ascll码 ord('a')
chr ascll码转字母 chr(97)
```
### 807二维数组 行列灵活使用

# day3

# day4

# day5 

# day6 前缀 和积差
# day7前缀和
### 左右元素和的差值
核心代码 
```python
 result=[0]*len(nums)
        total = sum(nums)
        left=0
        for i in range(len(nums)):
            right = total -left -nums[i]
            result[i]=abs(right-left)
            left+=nums[i]
```
思路总结
1.前缀和可以在每一轮中正常算出，知道前缀和和sum，自然就可以求出后缀和



